import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const keys = await prisma.globalSetting.findMany({where: {key: {contains: 'bkash'}}}); console.log(keys); } main().catch(console.error).finally(() => prisma.$disconnect());
